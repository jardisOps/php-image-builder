.idea/
.DS_Store
.claude

# Arbeitsunterlagen — nicht Teil des Repos. Was dauerhaft gilt, steht in
# README.md und support/HANDBOOK.md.
docs/

# Lokales buildx-Cache-Verzeichnis (CACHE_BACKEND=local, CACHE_DIR)
.buildx-cache/

# Vendor Dependencies
vendor/

**/*.tmp
**/*.cache
**/*.log
**/*.secret
*.local
